# LRU-cache-design
 🔁 LRU Cache System  The **LRU Cache System** is a Flask-based web application that demonstrates the implementation of an **Least Recently Used (LRU) cache** using efficient data structures. The project exposes cache operations through REST APIs and provides a simple web interface to visualize cache behavior in real time.

## 📌 Features
- Implements **LRU eviction policy**
- Supports **O(1)** time complexity for `get` and `put` operations
- Uses **HashMap + Doubly Linked List** internally
- Exposes cache operations via **Flask REST APIs**
- Interactive UI to observe cache state (MRU → LRU)

---

## 🛠️ Tech Stack
- **Backend:** Python, Flask
- **Frontend:** HTML, CSS, JavaScript
- **Data Structures:** HashMap, Doubly Linked List
- **Deployment:** Render (Gunicorn)

---

## 📂 Project Structure
